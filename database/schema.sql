 CREATE TABLE po_data (
    id INTEGER,
    internalid INTEGER,
    tranid VARCHAR(20),
    trandate VARCHAR(20),
    status VARCHAR(50),
    entity VARCHAR(20),
    entity_display VARCHAR(100),
    subsidiary VARCHAR(50),
    department VARCHAR(50),
    currency VARCHAR(10),
    exchangerate DECIMAL(10,2),
    approvalstatus VARCHAR(30),
    memo VARCHAR(200),
    line INTEGER,
    item VARCHAR(30),
    item_display VARCHAR(100),
    quantity DECIMAL(12,2),
    rate DECIMAL(12,2),
    amount DECIMAL(12,2),
    quantityreceived DECIMAL(12,2),
    quantitybilled DECIMAL(12,2),
    taxcode VARCHAR(30),
    location VARCHAR(50),
    class VARCHAR(50),
    department_line VARCHAR(50),
    expectedreceiptdate VARCHAR(20),
    closed VARCHAR(1),
    po_total DECIMAL(12,2),
    po_billed DECIMAL(12,2),
    po_remaining DECIMAL(12,2)
  );
